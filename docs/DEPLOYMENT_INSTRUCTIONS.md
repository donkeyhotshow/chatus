# 📋 ИНСТРУКЦИИ ПО РАЗВЕРТЫВАНИЮ

## 🔧 ПРЕДВАРИТЕЛЬНЫЕ ТРЕБОВАНИЯ

### 1. Установить Firebase CLI (если еще не установлен)
```bash
npm install -g firebase-tools
firebase login
```

---

## 📦 РАЗВЕРТЫВАНИЕ ИНДЕКСОВ FIRESTORE

### Применить индексы:
```bash
firebase deploy --only firestore:indexes
```

**Важно:** Индексы создаются асинхронно. Процесс может занять несколько минут.

**Проверка статуса:**
```bash
firebase firestore:indexes
```

---

## ☁️ РАЗВЕРТЫВАНИЕ CLOUD FUNCTIONS

### 1. Установить зависимости:
```bash
cd functions
npm install
```

### 2. Собрать TypeScript:
```bash
npm run build
```

### 3. Развернуть функции:
```bash
firebase deploy --only functions
```

**Или развернуть конкретную функцию:**
```bash
firebase deploy --only functions:cleanupEmptyRooms
firebase deploy --only functions:onRoomUpdate
```

### 4. Проверить логи:
```bash
firebase functions:log
```

---

## ✅ ПРОВЕРКА РАЗВЕРТЫВАНИЯ

### Проверить индексы:
1. Открыть Firebase Console
2. Firestore Database → Indexes
3. Убедиться, что все индексы созданы (статус "Enabled")

### Проверить функции:
1. Открыть Firebase Console
2. Functions
3. Убедиться, что функции развернуты и активны

---

## 🚀 АЛЬТЕРНАТИВНЫЙ СПОСОБ (через Firebase Console)

### Индексы:
1. Открыть Firebase Console
2. Firestore Database → Indexes
3. Нажать "Add Index"
4. Вручную создать индексы из `firestore.indexes.json`

### Функции:
1. Открыть Firebase Console
2. Functions
3. Нажать "Get Started" (если первый раз)
4. Следовать инструкциям для развертывания

---

## 📝 ПРИМЕЧАНИЯ

- Индексы необходимы для оптимизации запросов
- Cloud Functions требуют платный план Firebase (Blaze)
- Функции автоматически запускаются по расписанию
- Логи можно просматривать в Firebase Console

---

## 🔍 ОТЛАДКА

### Если индексы не создаются:
- Проверить синтаксис `firestore.indexes.json`
- Убедиться, что проект Firebase правильно настроен
- Проверить права доступа

### Если функции не развертываются:
- Проверить, что установлен Node.js 18+
- Проверить зависимости в `functions/package.json`
- Проверить логи сборки: `npm run build`

---

## ✅ ГОТОВО

После развертывания:
- ✅ Индексы оптимизируют запросы
- ✅ Функции автоматически очищают пустые комнаты
- ✅ Приложение готово к продакшену

