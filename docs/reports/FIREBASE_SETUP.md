# 🔥 Настройка Firebase для локальной разработки

## ⚠️ Текущая проблема

В `.env.local` файле стоят placeholder значения вместо реальных Firebase конфигурационных данных.

**Ошибка:** `Firebase: Error (auth/api-key-not-valid.-please-pass-a-valid-api-key.)`

---

## ✅ Решение: Получить реальные Firebase конфигурационные данные

### Шаг 1: Открыть Firebase Console

1. Перейдите на [Firebase Console](https://console.firebase.google.com/)
2. Войдите в свой Google аккаунт
3. Выберите проект (или создайте новый)

### Шаг 2: Получить конфигурацию Web App

1. В Firebase Console нажмите на **⚙️ (Settings)** → **Project settings**
2. Прокрутите вниз до секции **"Your apps"**
3. Найдите секцию **Web apps** (или создайте новое Web app, если его нет)
4. Нажмите на иконку **</>** (Web) или на название вашего Web app

### Шаг 3: Скопировать конфигурацию

Вы увидите код вида:

```javascript
const firebaseConfig = {
  apiKey: "AIzaSyC...",
  authDomain: "your-project.firebaseapp.com",
  projectId: "your-project-id",
  storageBucket: "your-project.appspot.com",
  messagingSenderId: "123456789",
  appId: "1:123456789:web:abcdef123456"
};
```

### Шаг 4: Обновить `.env.local`

Откройте файл `.env.local` в корне проекта и замените значения:

```env
NEXT_PUBLIC_FIREBASE_API_KEY=AIzaSyC... (ваш реальный API ключ)
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=123456789
NEXT_PUBLIC_FIREBASE_APP_ID=1:123456789:web:abcdef123456
```

**Важно:** 
- Замените все `your_*` значения на реальные из Firebase Console
- Не используйте кавычки в `.env.local`
- Значения должны быть без пробелов

---

## 🔧 Настройка Firebase сервисов

### 1. Authentication (Аутентификация)

1. В Firebase Console перейдите в **Authentication** → **Sign-in method**
2. Включите **Anonymous** (Анонимная аутентификация)
3. Нажмите **Enable** и **Save**

### 2. Firestore Database

1. Перейдите в **Firestore Database**
2. Нажмите **Create database**
3. Выберите режим:
   - **Start in test mode** (для разработки)
   - Или **Start in production mode** (для продакшена)
4. Выберите регион (например, `europe-west`)
5. Нажмите **Enable**

**Важно:** После создания базы данных разверните правила из файла `firestore.rules`:
1. Перейдите в **Firestore Database** → **Rules**
2. Скопируйте содержимое файла `firestore.rules` из проекта
3. Вставьте в редактор правил
4. Нажмите **Publish**

### 3. Storage

1. Перейдите в **Storage**
2. Нажмите **Get started**
3. Выберите режим (test mode или production mode)
4. Выберите регион
5. Нажмите **Done**

**Важно:** Разверните правила из файла `storage.rules`:
1. Перейдите в **Storage** → **Rules**
2. Скопируйте содержимое файла `storage.rules` из проекта
3. Вставьте в редактор правил
4. Нажмите **Publish**

---

## 🔄 После обновления `.env.local`

### Перезапустить dev-сервер

1. Остановите текущий сервер (Ctrl+C)
2. Запустите заново:
   ```bash
   npm run dev
   ```

**Важно:** Next.js загружает переменные окружения только при запуске. После изменения `.env.local` нужно перезапустить сервер.

---

## ✅ Проверка

После настройки:

1. Откройте http://localhost:9002
2. Откройте DevTools (F12) → Console
3. Не должно быть ошибок Firebase
4. Попробуйте создать комнату - должно работать

---

## 📝 Пример правильного `.env.local`

```env
# Firebase Configuration
NEXT_PUBLIC_FIREBASE_API_KEY=AIzaSyC1234567890abcdefghijklmnopqrstuvwxyz
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=my-chat-app.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=my-chat-app
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=my-chat-app.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=987654321
NEXT_PUBLIC_FIREBASE_APP_ID=1:987654321:web:abcdef1234567890
```

---

## 🐛 Troubleshooting

### Ошибка все еще появляется

1. **Проверьте формат `.env.local`:**
   - Нет лишних пробелов
   - Нет кавычек вокруг значений
   - Каждая переменная на новой строке

2. **Перезапустите сервер:**
   ```bash
   # Остановите (Ctrl+C)
   npm run dev
   ```

3. **Проверьте, что переменные загружаются:**
   - Откройте DevTools → Console
   - Введите: `console.log(process.env.NEXT_PUBLIC_FIREBASE_API_KEY)`
   - Должно показать ваш API ключ (не `your_api_key`)

4. **Проверьте Firebase Console:**
   - Убедитесь, что проект активен
   - Проверьте, что Web app создан
   - Проверьте, что API ключ правильный

### "Permission denied" ошибки

- Разверните Firestore Rules из `firestore.rules`
- Разверните Storage Rules из `storage.rules`

---

*Инструкция создана: 2025-01-30*


