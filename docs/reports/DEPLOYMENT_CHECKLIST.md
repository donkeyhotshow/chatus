# 🚀 Чек-лист деплоя ChatForUs

**Дата:** 2025-12-12
**Проект:** ChatForUs
**Версия:** 0.1.0

---

## ✅ Статус готовности: ГОТОВ К ДЕПЛОЮ

Проект успешно консолидирован и протестирован согласно DEPLOY_READY_REPORT.md.

---

## 📋 Предварительные требования

### 1. Firebase проект
- [ ] Создать Firebase проект в [Firebase Console](https://console.firebase.google.com/)
- [ ] Включить Authentication (Anonymous)
- [ ] Создать Firestore Database (режим production)
- [ ] Создать Storage Bucket
- [ ] Развернуть правила безопасности:
  ```bash
  firebase deploy --only firestore:rules,storage
  ```

### 2. Переменные окружения
Создать `.env.local` на основе `env.example`:

```env
NEXT_PUBLIC_FIREBASE_API_KEY=ваш_api_key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=ваш-project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=ваш-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=ваш-project.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=ваш_sender_id
NEXT_PUBLIC_FIREBASE_APP_ID=ваш_app_id
```

### 3. GitHub (для автоматизированного деплоя)
- [ ] Создать репозиторий на GitHub
- [ ] Добавить секреты в Settings > Secrets and variables > Actions:
  - `VERCEL_TOKEN` - токен Vercel API
  - `FIREBASE_SERVICE_ACCOUNT` - сервисный аккаунт Firebase (JSON)

---

## 🛠️ Варианты деплоя

### Вариант 1: Vercel + Firebase (Рекомендуемый)

#### Автоматический деплой через GitHub Actions
1. Запустить скрипт настройки секретов:
   ```bash
   .\setup-github-secrets.ps1
   ```
2. Загрузить код на GitHub
3. Запустить workflow "Deploy Firebase and Vercel" в Actions

#### Ручной деплой
```bash
# Firebase rules
firebase deploy --only firestore:rules,storage

# Vercel
vercel --prod
```

### Вариант 2: Firebase App Hosting (Альтернативный)

```bash
# Создать backend
firebase apphosting:backends:create

# Деплой
firebase deploy --only hosting
```

---

## 🔧 Пост-деплой проверки

### Vercel
- [ ] Приложение доступно по URL
- [ ] Нет ошибок в логах (Vercel Dashboard > Functions)
- [ ] Environment variables установлены корректно

### Firebase
- [ ] Firestore Database активна
- [ ] Storage Bucket создан
- [ ] Правила безопасности развернуты
- [ ] Authentication работает

### Функциональность
- [ ] Создание комнат работает
- [ ] Вход в комнаты работает
- [ ] Чат функционирует
- [ ] Мини-игры работают
- [ ] Canvas коллаборация работает

---

## 🚨 Важные замечания

### Режим демо
- **Отключить** `NEXT_PUBLIC_DEMO_MODE=true` в продакшене
- Демо-режим предназначен только для локальной разработки

### Безопасность
- Все Firebase правила проверены и протестированы
- Rate limiting реализован
- Валидация данных на уровне приложения

### Производительность
- Bundle size оптимизирован (<200KB)
- PWA настроен с service worker
- Image optimization включен

---

## 📞 Поддержка

При проблемах с деплоем:

1. Проверить логи в Vercel/Firebase консолях
2. Убедиться, что все environment variables установлены
3. Проверить Firebase правила безопасности
4. Свериться с документацией в папке `docs/`

---

*Чек-лист создан автоматически на основе анализа проекта ChatForUs*


