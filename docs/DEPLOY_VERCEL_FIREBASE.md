# 🚀 Деплой ChatForUs на Vercel + Firebase

## ✅ Рекомендуемый вариант: Vercel + Firebase

### Почему этот вариант:
- ✅ Next.js 15.5.7 идеально работает на Vercel (SSR, API Routes, Static Generation)
- ✅ Firebase уже интегрирован (Auth, Firestore, Storage)
- ✅ Бесплатный тариф Vercel достаточен для MVP (1 комната, 2 пользователя)
- ✅ Автоматический CI/CD через GitHub
- ✅ PWA поддержка уже настроена

---

## 📋 Подготовка к деплою

### 1. Настройка Firebase

#### 1.1 Получить Firebase конфигурацию

1. Откройте [Firebase Console](https://console.firebase.google.com/)
2. Выберите проект (или создайте новый)
3. Перейдите в **Project Settings** → **General**
4. Найдите секцию **Your apps** → **Web app**
5. Скопируйте конфигурацию Firebase:

```javascript
const firebaseConfig = {
  apiKey: "AIza...",
  authDomain: "your-project.firebaseapp.com",
  projectId: "your-project-id",
  storageBucket: "your-project.appspot.com",
  messagingSenderId: "123456789",
  appId: "1:123456789:web:abcdef"
};
```

#### 1.2 Настроить Firestore Rules

Убедитесь, что `firestore.rules` настроены правильно (уже есть в проекте).

#### 1.3 Настроить Storage Rules

Убедитесь, что `storage.rules` настроены правильно (уже есть в проекте).

---

### 2. Настройка переменных окружения

#### 2.1 Локально (для разработки)

Создайте файл `.env.local` в корне проекта:

```env
NEXT_PUBLIC_FIREBASE_API_KEY=AIza...
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=123456789
NEXT_PUBLIC_FIREBASE_APP_ID=1:123456789:web:abcdef
```

#### 2.2 На Vercel

1. Откройте проект на [Vercel Dashboard](https://vercel.com/dashboard)
2. Перейдите в **Settings** → **Environment Variables**
3. Добавьте все переменные из `.env.local`:
   - `NEXT_PUBLIC_FIREBASE_API_KEY`
   - `NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN`
   - `NEXT_PUBLIC_FIREBASE_PROJECT_ID`
   - `NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET`
   - `NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID`
   - `NEXT_PUBLIC_FIREBASE_APP_ID`

**Важно:** Выберите окружения:
- ✅ Production
- ✅ Preview
- ✅ Development

---

### 3. Подготовка проекта

#### 3.1 Проверка зависимостей

```bash
npm install
```

#### 3.2 Проверка сборки локально

```bash
npm run build
```

Если сборка успешна, можно деплоить.

---

## 🚀 Деплой на Vercel

### Вариант 1: Через Vercel CLI (рекомендуется для первого деплоя)

#### 1. Установка Vercel CLI (если не установлен)

```bash
npm i -g vercel
```

#### 2. Авторизация

```bash
vercel login
```

#### 3. Деплой

```bash
# Первый деплой (создаст проект)
vercel

# Последующие деплои
vercel --prod
```

**Во время первого деплоя:**
- Выберите scope (команду)
- Введите название проекта (например: `chatforus`)
- Подтвердите настройки

### Вариант 2: Через GitHub (рекомендуется для CI/CD)

#### 1. Подключение репозитория

1. Откройте [Vercel Dashboard](https://vercel.com/dashboard)
2. Нажмите **Add New Project**
3. Выберите репозиторий GitHub
4. Настройте проект:
   - **Framework Preset:** Next.js (автоопределение)
   - **Root Directory:** `./` (если проект в корне)
   - **Build Command:** `npm run build` (по умолчанию)
   - **Output Directory:** `.next` (по умолчанию)
   - **Install Command:** `npm install` (по умолчанию)

#### 2. Настройка переменных окружения

Добавьте все Firebase переменные в **Environment Variables** (см. раздел 2.2).

#### 3. Деплой

После подключения репозитория:
- Каждый push в `main` → автоматический деплой в Production
- Каждый push в другие ветки → Preview деплой
- Pull Request → Preview деплой с комментарием

---

## 🔧 Конфигурация Vercel

### vercel.json (уже настроен)

```json
{
  "buildCommand": "npm run build",
  "devCommand": "npm run dev",
  "installCommand": "npm install",
  "framework": "nextjs",
  "regions": ["iad1"]
}
```

### Дополнительные настройки (опционально)

Если нужно настроить домен или другие параметры:

```json
{
  "buildCommand": "npm run build",
  "devCommand": "npm run dev",
  "installCommand": "npm install",
  "framework": "nextjs",
  "regions": ["iad1"],
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/:path*"
    }
  ],
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "X-Content-Type-Options",
          "value": "nosniff"
        },
        {
          "key": "X-Frame-Options",
          "value": "DENY"
        },
        {
          "key": "X-XSS-Protection",
          "value": "1; mode=block"
        }
      ]
    }
  ]
}
```

---

## 📱 PWA настройка

PWA уже настроена через `@ducanh2912/next-pwa`. После деплоя:

1. Откройте сайт на мобильном устройстве
2. Добавьте на главный экран
3. PWA будет работать офлайн (с кэшированием)

---

## 🔍 Проверка деплоя

### 1. Проверка Production URL

После деплоя получите URL:
- Production: `https://your-project.vercel.app`
- Preview: `https://your-project-git-branch-username.vercel.app`

### 2. Проверка Firebase подключения

1. Откройте сайт
2. Откройте DevTools → Console
3. Проверьте, что нет ошибок Firebase
4. Попробуйте создать комнату и отправить сообщение

### 3. Проверка Realtime

1. Откройте сайт в двух вкладках
2. Создайте комнату в первой вкладке
3. Присоединитесь во второй вкладке
4. Отправьте сообщение - должно появиться в обеих вкладках

---

## 🐛 Troubleshooting

### Ошибка: "Firebase config not found"

**Причина:** Переменные окружения не настроены

**Решение:**
1. Проверьте `.env.local` локально
2. Проверьте Environment Variables на Vercel
3. Убедитесь, что переменные начинаются с `NEXT_PUBLIC_`

### Ошибка: "Permission denied" в Firestore

**Причина:** Firestore Rules не настроены

**Решение:**
1. Откройте Firebase Console → Firestore Database → Rules
2. Убедитесь, что правила развернуты
3. Проверьте `firestore.rules` в проекте

### Ошибка: "Storage permission denied"

**Причина:** Storage Rules не настроены

**Решение:**
1. Откройте Firebase Console → Storage → Rules
2. Убедитесь, что правила развернуты
3. Проверьте `storage.rules` в проекте

### Деплой падает с ошибкой сборки

**Решение:**
1. Проверьте логи сборки на Vercel
2. Попробуйте собрать локально: `npm run build`
3. Проверьте версии Node.js (Vercel использует 22.x по умолчанию)

---

## 📊 Мониторинг

### Vercel Analytics (опционально)

1. Откройте проект на Vercel Dashboard
2. Перейдите в **Analytics**
3. Включите Web Analytics (бесплатно для Hobby плана)

### Firebase Analytics (опционально)

1. Откройте Firebase Console
2. Перейдите в **Analytics**
3. Следуйте инструкциям для настройки

---

## 🔄 Обновление проекта

### Автоматические обновления (через GitHub)

1. Сделайте изменения в коде
2. Закоммитьте и запушьте в `main`
3. Vercel автоматически задеплоит новую версию

### Ручной деплой

```bash
vercel --prod
```

---

## 💰 Стоимость

### Vercel (Hobby - бесплатно)
- ✅ Неограниченные деплойменты
- ✅ Неограниченная пропускная способность
- ✅ 100GB bandwidth/месяц
- ✅ SSL сертификаты
- ✅ Preview деплойменты

### Firebase (Spark - бесплатно)
- ✅ Firestore: 1GB storage, 50K reads/день, 20K writes/день
- ✅ Storage: 5GB storage, 1GB downloads/день
- ✅ Auth: неограниченно
- ✅ Realtime: неограниченно

**Для MVP (1 комната, 2 пользователя):** ✅ Достаточно бесплатных тарифов

---

## ✅ Чеклист перед деплоем

- [ ] Firebase проект создан и настроен
- [ ] Firestore Rules развернуты
- [ ] Storage Rules развернуты
- [ ] Переменные окружения добавлены в Vercel
- [ ] Локальная сборка успешна (`npm run build`)
- [ ] Репозиторий подключен к Vercel (или готов к деплою через CLI)
- [ ] PWA манифест настроен (уже есть)

---

## 🎉 Готово!

После выполнения всех шагов ваш проект будет доступен по адресу:
**https://your-project.vercel.app**

---

*Документ создан: 2025-01-30*

