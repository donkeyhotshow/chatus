# Copy this file to `.env.local` and fill with your Firebase project values.
# Do NOT commit `.env.local` to source control.

# Firebase public config (used on the client). Values are required for real Firebase usage.
NEXT_PUBLIC_FIREBASE_API_KEY=your_api_key_here
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_messaging_sender_id
NEXT_PUBLIC_FIREBASE_APP_ID=your_app_id

# If you want to run the app without a Firebase backend (demo mode), you can set:
# DEMO_MODE=true

# GitHub Token (для автоматизированной загрузки проекта на GitHub)
# Получите токен: https://github.com/settings/tokens (classic token с repo scope)
GH_TOKEN=your_github_personal_access_token

# Note:
# - Ensure there are no trailing CR/LF characters in values (the project sanitizes env values).
# - The real `.env.local` file is ignored by .gitignore.

