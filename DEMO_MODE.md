# 🎭 Режим демо для локального тестирования

Режим демо позволяет тестировать приложение локально **без настройки Firebase**.

---

## ✅ Быстрое включение

### Вариант 1: Через переменную окружения (рекомендуется)

Добавьте в файл `.env.local`:

```env
NEXT_PUBLIC_DEMO_MODE=true
```

Затем перезапустите dev сервер:

```bash
npm run dev
```

### Вариант 2: Через localStorage (в браузере)

Откройте консоль браузера (F12) и выполните:

```javascript
localStorage.setItem('demo_mode', 'true');
location.reload();
```

---

## 🎯 Что работает в демо-режиме

✅ **Работает:**
- Создание комнат
- Вход в комнаты
- Создание профиля пользователя
- Базовый интерфейс чата
- Навигация по приложению

❌ **Не работает (требует Firebase):**
- Отправка сообщений в реальном времени
- Синхронизация между вкладками/устройствами
- Загрузка изображений
- Коллаборативный canvas
- Мини-игры
- Онлайн статус пользователей

---

## 🔧 Отключение демо-режима

### Вариант 1: Удалить переменную окружения

Удалите или закомментируйте строку в `.env.local`:

```env
# NEXT_PUBLIC_DEMO_MODE=true
```

### Вариант 2: Через localStorage

В консоли браузера:

```javascript
localStorage.removeItem('demo_mode');
location.reload();
```

---

## 📝 Примечания

- Демо-режим использует localStorage для хранения данных
- Данные не синхронизируются между вкладками
- При перезагрузке страницы данные сохраняются
- Для полного тестирования функциональности настройте Firebase

---

## 🚀 Для продакшена

**Важно:** Демо-режим должен быть **отключен** в продакшене. Убедитесь, что:

1. `NEXT_PUBLIC_DEMO_MODE` не установлен в переменных окружения Vercel
2. Firebase конфигурация настроена правильно
3. Все функции протестированы с реальным Firebase

---

*Демо-режим создан для удобства локальной разработки и тестирования UI*

